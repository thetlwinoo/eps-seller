import { StockItemsFilterPipe } from './stock-items-filter.pipe';

describe('StockItemsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StockItemsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
