import { environment } from '@app/env';

export const SERVER_API_URL = `${environment.serverApi.url}`;
export const DEBUG_INFO_ENABLED = false;
export const VERSION = 'v1';
export const BUILD_TIMESTAMP = null;
